# Instructions

## File locations

* All docs go in the docs folder
* All tests and debug scripts go in the storelocator-list/tests folder

## Development Environment

* WordPress runs in Docker (no local PHP installation)
* Update the Docker WordPress instance by running `update.sh` script
* Use browser-based testing and debugging scripts in the tests folder
* All testing should be done via web browser accessing the Docker instance
